<table class="table table-hover">
  <thead>
    <tr>
      <th>名前</th>
    </tr>
  </thead>
  <tbody>
    <% users.each do |user| %>
      <tr>
        <td>
          <%= link_to user.name, user_path(user), class: "text-dark" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>