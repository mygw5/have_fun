<!DOCTYPE html>
<html>
  <head>
    <title>Let's have fun!</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/f94369044a.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <!-- controller単位でレイアウトを調整するため -->
    <%= stylesheet_link_tag params[:controller], 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'rails_admin', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'rails_admin', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <header>
      <%= render 'layouts/header' %>
    </header>

    <main>
      <% flash.each do |message_type, message| %>
        <div class="flash-message <%= message_type %>"><%= message %></div>
      <% end %>
      <div class="container-fuild">
        <%= yield %>
      </div>
    </main>

    <footer>

    </footer>
  </body>
</html>
