<div class="row py-3">
  <h3><span>マイリスト</span></h3>
</div>

<div class="row pt-5">
  <% if @post_hobbies.present? %>
    <div class="col-12">
      <%= render 'index',  post_hobbies: @post_hobbies %>
    </div>
  <% else %>
    <p class="mx-auto">お気に入り登録がありません</p>
  <% end %>
</div>

<%= javascript_pack_tag 'script/fadein' %>