<%= search_form_for q do |f| %>
  <%= f.search_field :title_cont, placeholder: "キーワードを入力" %>
  <%= f.label :tag_id, 'タグ' %>
  <%= f.collection_select :tag_id_eq, Tag.where.not(id: 0), :id, :tag_name,  include_blank: 'タグを選択' %>
  <%= f.submit '検索' %>
<% end %>
